# 里程碑追踪矩阵

状态：`planned`
产品范围：[项目计划.md](../项目计划.md)
阶段入口：[spec/README.md](./spec/README.md)
人工决策：[todoe-check.md](../todoe-check.md)

## 1. 纵向链路

```text
M0 项目基础设施
→ M1 小说导入
→ M2 文本 AI 处理
→ M3 角色系统
→ M4 语音生成
→ M5 音频 QA
→ M6 章节合成
→ M7 同步阅读
→ M8 工程导入导出
```

里程碑表示可验收的纵向能力，不表示所有任务只能串行。只有共享 schema、index、package.json、锁文件、路由或同一写入文件的任务必须串行。

## 2. 输入输出与完成证据

| 里程碑 | 阶段 | 主要输入 | 稳定输出 | 完成证据 | 当前状态 |
|---|---|---|---|---|---|
| M0 | 00 | 空工作区、项目目录 | Project、状态库、Artifact/Task/Dependency/Review 端口 | 后端基础设施测试；桌面入口仍单列 | 里程碑：`verified`；阶段 00：`in_progress` |
| M1 | 01 | TXT SourceAsset | raw/canonical/normalized、Block、Chapter、locator、覆盖率 | TXT 冻结夹具、重导入、桌面审核；EPUB 分支 `not_applicable` | `planned` |
| M2 | 01 桥接、02～04 | normalized、Chapter | Scene、ProcessingSegment、corrected、ScriptUnit、Character/Alias/Stage | Mock+批准 Provider、审核、局部 stale | `planned` |
| M3 | 04～05 | Character/Stage、ScriptUnit | VoiceAsset/Profile/Pool、Assignment | 角色稳定、Scene 普通角色可区分 | `planned` |
| M4 | 06～07 | ScriptUnit、VoiceAssignment | spoken、Pronunciation、AudioClip | 单单元生成、缓存、重试、live TTS | `planned` |
| M5 | 08 | AudioClip、spoken | QAResult、Review、回退目标 | ASR/音频检测、异常路由、人工复核 | `planned` |
| M6 | 09 | approved AudioClip | ChapterAssembly、章节音频 | 完整播放、可反查 ScriptUnit、确定性重建 | `planned` |
| M7 | 10 | ChapterAssembly、ScriptUnit | ChapterTimeline、JSON/WebVTT/SRT | 同步播放、时间范围和导出验证 | `planned` |
| M8 | 11 | 项目 revisions、发布快照 | 工程包、导入结果、发布成品/清单 | 重绑小说、恢复配置、checksum/兼容性 | `planned` |

M0 的“里程碑实现状态”与“阶段 00 整体完成状态”分开记录：前者已验证后端基础设施，后者仍等待 Electron 项目页、最近项目和跨进程接线。

## 3. Gate 汇总

| 里程碑 | 必须关闭的人工决策段 | 可在 Gate 前执行 |
|---|---|---|
| ARCH | `TODO-ARCH-01`～`03` | M1～M8 按各自 Gate 继续；不得宣称目标平台发布已验收，也不得实现配额/自动清理 |
| M1 | `TODO-M1-01`～`09`；`07` 只在最终验收前关闭 | 只读审计、无业务包骨架 |
| M2 | `TODO-M2-01`～`20`，并复用 M1 Scene 结论；`20` 只在最终验收前关闭 | M2-00、M2-01A、M2-01B、M2-02A、M2-02B：只读审计、Scene/ProcessingSegment 桥接、Provider/说话人包骨架与 workspace 依赖 |
| M3 | `TODO-M3-*` | M3-00、M3-01A、M3-01B：只读审计、tts-engine 包骨架与 workspace 依赖 |
| M4 | `TODO-M4-*` | M4-00、M4-01A、M4-01B：只读审计、pronunciation 包骨架与 workspace 依赖 |
| M5 | `TODO-M5-*` | M5-00、M5-01A：只读审计与 asr-engine 包骨架 |
| M6 | `TODO-M6-*` | M6-00、M6-01A、M6-01B：只读审计、audio-processing 扩展基线与 workspace 依赖 |
| M7 | `TODO-M7-*` | 只读审计、timeline 包骨架与 workspace 依赖接线；字幕契约/序列化/夹具等待 M7-D01 closed |
| M8 | `TODO-M8-*` | M8-00、M8-01A、M8-01B：只读审计、project-transfer 包骨架与 workspace 依赖 |

具体可执行范围以对应里程碑规格的 Decision Gate 为准；本矩阵不放宽任何 Gate。

### 3.1 决策基线

`decision-baseline-2026-08-08` 及 TODO-M1-06 的 2026-08-09 追加确认已冻结全部 TODO 当前唯一勾选候选。候选冻结不等于补齐外部执行输入，也不关闭 G00 上游实现门禁；TODO-M1-06 的预算数值未齐，因此 M1-D05 仍保持 open。

| 里程碑 | 已冻结的主要分支 | 仍需独立补齐的执行输入 |
|---|---|---|
| ARCH | 单一正式平台；单一打包工具；软配额且正式 revision 不自动删除 | OS/最低版本/CPU/安装包；`engines`/`.nvmrc`/ADR 0002 的 Node 支持范围解释与 Electron 内置 Node/`node:sqlite` 兼容证据；打包/签名/更新 ADR 与发布输入；配额、保护线、保留期与恢复窗口 |
| M1 | Scene/Segment 移交 M2；只做 TXT；复制输入；Markdown 后续 | TXT 编码白名单、byte range/ID 细则、TXT 资源预算、私有基准证据 |
| M2 | M2 建 Scene/Segment；连续 slices；SAFE 自动应用；精确 `scriptUnitIds` | LLM Provider/ADR/live profile；SAFE policy；说话人阈值和校准样章 |
| M3 | 核心标签加扩展；自动质量检查加人工试听；精确影响展开 | rights/quality 字段；检查器与区分度实现、版本、预算和测试命令 |
| M4 | spoken+pinyin；JSON 词典；单一 TTS Provider；本地结果校验 | TTS Provider/ADR/live profile；WAV/PCM 规格；并发/费用/队列、资产和校验预算 |
| M5 | 外部非模型音频 API；保守文本 diff；相似度仅提示 | ASR/VAD/diarization 开关与 Provider；外部检测 API；QA/响度阈值和 live 证据 |
| M6 | 随应用分发受控本地处理器；单轨；无插件 SDK | 处理器 ADR/版本/许可/smoke；共享音频规格和响度/停顿 profile |
| M7 | corrected 主显示、spoken 可查看；ScriptUnit 级；时间轴只读 | 字幕 profile 的 `subtitle_text_layer`、缺失行为、精确转义、时间格式和兼容夹具 |
| M8 | ZIP+JSON+checksums；不加密；只导入新项目；不签名/不分块 | 容器依赖与预算；rights 字段；发布工具链、播放器矩阵和 golden fixture |

## 4. 完成传播

- 上游里程碑完成只证明其稳定输出，不自动证明下游已实现。
- 上游 revision 变化时，下游只按依赖 selector 标记实际交集为 stale。
- 里程碑验收失败必须返回具体任务，不在验收卡修改 expected 或降低阈值。
- M8 完成后仍需使用阶段 90 的合法样章执行一次 M0～M8 纵向审计。
