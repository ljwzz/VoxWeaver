# M7：同步阅读执行规格

状态：`planned`
当前门禁：`blocked_by_M6`
适用执行者：上下文较小、一次只执行一个任务卡的大语言模型
对应阶段：[阶段 10：时间轴与同步阅读](../spec/10.时间轴与同步阅读.md)
上游里程碑：[M6：章节合成执行规格](./M6.章节合成执行规格.md)
无人值守协议：[无人值守执行协议](../无人值守执行协议.md)
人工决策入口：[todoe-check.md](../../todoe-check.md)

## 1. 文档定位

本文件把 M7 拆成可独立实现、验证和交接的任务卡，不替代项目主计划、阶段 10、阶段 90、ADR 或机器可校验 schema。

权威顺序统一遵循 [无人值守执行协议第 2 节](../无人值守执行协议.md#2-权威顺序)；下列只是 M7 相关读取顺序：

1. [项目计划](../../项目计划.md) 的 M7 完成定义；
2. [阶段 10](../spec/10.时间轴与同步阅读.md) 的范围和验收；
3. [阶段 90](../spec/90.MVP联调与验收.md) 的样章闭环；
4. 已接受 ADR 和已冻结 schema；
5. [Electron 架构规格](../architecture/electron-desktop-architecture.md) 中与前述权威输入一致的传输、资源访问和项目隔离；
6. 本文件的任务顺序、写入白名单和验证命令。

冲突时停止并上报，执行模型不得自行修改上位范围或填写人工决策。

## 2. 范围与非目标

### 2.1 M7 范围

- 从一个当前 `ChapterAssembly` revision 生成稳定的 ScriptUnit 级时间轴；
- 保存每个条目的章节、ScriptUnit、角色、显示文本、ClipRef 和整数毫秒范围；
- 校验时间范围单调、非负、不越过章节时长并可双向反查；
- JSON 作为项目内完整契约，WebVTT 和 SRT 作为派生兼容导出；
- 提供播放、暂停、倍速、拖动、章节切换和当前 ScriptUnit 高亮；
- 支持点击文本跳转音频、音频进度定位文本；
- 显示 `stale`、缺失、部分生成和项目切换状态；
- 保存 timeline revision、输入 assembly revision、序列化器版本和输出 hash。

### 2.2 非目标

- 不在 MVP 实现逐字或逐词强制对齐；
- 不修改章节音频、ScriptUnit、角色注册表或上游文本；
- 不把 WebVTT/SRT 回导为项目真值；
- 不实现移动端、网页端、云播放器、在线协作、书签或笔记；
- 不让 Renderer 读取绝对路径或跨项目复用旧播放状态；
- 不因显示样式变化使数据时间轴或音频过期；
- 不把角色头像、主题、Scene 面板等增强能力塞入 M7。

## 3. 小模型执行协议

每次只执行一个任务 ID。派发上下文只包含本文件第 2～6 节、选中任务卡、已关闭门禁证据、涉及接口和任务开始时的 `git status --short`。

执行规则：

- 先核对前置任务、上游 Artifact/schema 版本和 Decision Gate；
- 只修改任务卡的精确白名单；
- 先写失败测试或冻结夹具，再写最小实现；
- 公共类型从目标包 `src/index.ts` 导出；测试沿用 `test/*.test.mjs` 并从 `dist` 导入；
- 不修改主计划、`docs/spec/`、ADR、`docs/ideas/`、其他里程碑规格或根待办；
- 不新增依赖，除非任务卡和已关闭门禁明确允许；
- 不执行 Git 暂存、提交、重置、清理或无关格式化；
- 单元测试通过不得代替真实播放与桌面交互证据。

任务结束前运行 `git diff --check` 和 `git status --short`，并按 M6 的交接字段报告结果、文件、验证、未验证、既有改动和阻塞。

## 4. 公共不变量

- `ChapterTimeline` 必须绑定一个明确、不可变的 `chapterAssemblyRevisionId`；
- JSON timeline 是项目真值；WebVTT/SRT 只能从同一 revision 派生，不能反向覆盖 JSON；
- 公共范围使用整数毫秒半开区间 `[startMs,endMs)`；条目按 `startMs`、`order` 稳定排序；
- 每个条目必须反查到 `sourceClipRefId`、`audioArtifactRevisionId` 和 `scriptUnitId`；
- 范围非负、开始小于结束、不越过章节时长；是否允许重叠必须由契约明确，不能由播放器偶然行为决定；
- 显示文本层、角色前缀和时间舍入策略必须版本化并进入 timeline/export 指纹；
- speaker 显示名变化不得改变音频时间，必要时只生成新的显示导出 revision；
- assembly revision 变化只使对应章节 timeline 和字幕过期；
- 章节顺序变化只影响全书目录/发布清单，不使未变单章 timeline 过期；
- 序列化必须确定性；相同输入、schema 和 serializer version 产生相同字节和 hash；
- 生成结果先写任务 tmp，校验后才提交；旧 timeline 和字幕不覆盖；
- 播放状态至少按 `projectSessionId + chapterId + timelineRevisionId` 隔离；项目切换后停止旧音频并拒绝旧事件；
- Renderer 只使用受控 artifact URL，不持有绝对路径，不自行推断正式 revision；
- 未关闭门禁时不得用默认显示层、前缀或编辑行为继续实现。

统一错误码至少包括：

```text
TIMELINE_ASSEMBLY_NOT_CURRENT
TIMELINE_INPUT_INCOMPLETE
TIMELINE_RANGE_INVALID
TIMELINE_MAPPING_MISSING
TIMELINE_SERIALIZATION_INVALID
TIMELINE_BASELINE_CONFLICT
TIMELINE_STALE_SESSION
TIMELINE_EXPORT_FAILED
TIMELINE_CANCELED
```

## 5. Decision Gates

### M7-G00：M6 输入与播放基础门禁

状态：`open`，阻塞 M7-01A 及以后任务。

未关闭时仍可执行：M7-00。

关闭条件：

- M6 已交付当前 ChapterAssembly revision、有序 ClipRef、章节音频、整数毫秒范围、总时长和反向映射；
- 缺失、未批准、拒绝或 stale Clip 已在 M6 阻断；
- M0/M6 的受控 artifact URL、媒体范围读取、项目 session 失效和桌面传输有测试证据；
- ScriptUnit 当前 revision 提供可引用的 corrected/spoken 与 speaker ID；
- `pnpm run check` 通过。

M7-00 可在门禁打开时只读执行；执行模型不得自行更新 M6 状态。

### M7-D01：显示文本、字幕前缀与时间格式策略

状态：`open`，引用 [TODO-M7-01](../../todoe-check.md#todo-m7-01)，直接阻塞 M7-02、M7-06、M7-07、M7-19 和 M7-20；M7-03、M7-08、M7-09 等后继以任务卡前置和 DAG 推导。

已确认策略（2026-08-08）：阅读器主文本使用 `corrected`，可显式切换查看 `spoken`；JSON 保留结构化 speaker 引用；WebVTT/SRT 角色前缀只由版本化导出 profile 决定，不写入 timeline 时间真值。

未关闭时仍可执行：M7-00、M7-01A、M7-01B；每卡仍须满足 M7-G00 和其他 Gate。

上述阅读器主显示/查看文本层选择不再是阻塞项；WebVTT/SRT 的 `subtitle_text_layer` 尚未固定。Gate 仍保持 `open`。

剩余关闭输入：

- `corrected`/`spoken` 缺失或 revision 不一致时的显示/阻断行为；
- JSON speaker 字段的稳定 ID/显示名投影，以及 WebVTT/SRT profile 的 ID/version、`subtitle_text_layer`（`corrected` 或 `spoken`）、所选 revision 缺失/不一致时的阻断行为、默认 profile、有前缀/无前缀策略、前缀模板和转义规则；
- 整数毫秒到 WebVTT/SRT 时间戳的舍入、章节边界和超长时长规则；
- 项目自有 SRT 兼容 profile 的换行、cue 编号、空行和文本换行规则；
- 显示名变化时重建哪些派生导出，哪些时间数据保持 current。

不得以某个本机播放器的偶然容忍行为代替明确 profile。

### M7-D02：对齐粒度与时间轴编辑权限

状态：`closed`，引用 [TODO-M7-02](../../todoe-check.md#todo-m7-02)。

已确认策略（2026-08-08）：MVP 只保证 ScriptUnit 级对齐，timeline 只读；不定义 timeline override 或时间编辑命令。需要改时间时，仅以章节/assembly/ScriptUnit 逻辑 ID 导航回 M6 重新装配，不传递绝对路径或直接改 M6 产物。

阻塞任务：无。M7-02、M7-04、M7-14、M7-19 和 M7-20 按上述只读分支执行。

若未来批准字/词级对齐或直接编辑时间轴，必须先更新上位范围、定义反向影响和新增任务，不得由现有卡片顺手实现。

## 6. 任务依赖 DAG

```text
M7-00 → M7-G00
M7-G00 closed → M7-01A → M7-01B
M7-01B + M7-D01 closed + M7-D02 closed → M7-02 → M7-03
M7-02 + M7-03 + M7-D02 closed → M7-04 → M7-05
M7-02 + M7-D01 closed → M7-06
M7-05 + M7-D01 closed → M7-07 → M7-08
M7-06 + M7-07 + M7-08 → M7-09
M7-04 + M7-05 + M7-06 + M7-07 + M7-08 + M7-09 → M7-10 → M7-11
M7-02 + M7-05 → M7-12
M7-11 + M7-12 → M7-13
M7-02 + M7-D02 closed → M7-14
M7-11 + M7-13 + M7-14 → M7-15 → M7-15B → M7-16 → M7-17
M7-05 + M7-17 → M7-18
M7-18 + M7-D01 closed + M7-D02 closed → M7-19
M7-19 + M7-D01 closed + M7-D02 closed → M7-20
```

M7-15B 必须位于 M7-15 与 M7-16 之间，不得与二者并行。共享 `src/index.ts`、`services/app-core/src/appCoreService.ts`、package manifest、锁文件和桌面路由的任务必须串行。带字母后缀的每个 ID 均按一张独立任务卡计数，M7-15 与 M7-15B 是两张任务卡；任务卡的“前置”是依赖真值，图只提供总览。

## 7. 原子任务卡

### M7-00：执行基线只读审计

单一结果：证明 M7-G00、M7-D01 和 M7-D02 当前是否满足，不修改文件。

- 前置：无。
- 读取：M6 规格/验收、ChapterAssembly/ScriptUnit 契约、artifact playback 实现与测试、桌面传输。
- 允许写入：无。
- 验证：`pnpm --filter @voxweaver/contracts test`；`pnpm --filter @voxweaver/audio-processing test`；`pnpm --filter @voxweaver/application test`；`pnpm --filter @voxweaver/app-core test`；`pnpm --dir apps/desktop/main test`；`pnpm --dir apps/desktop/preload test`；`pnpm run check`，记录退出码和未覆盖项。
- 停止条件：不得为关闭门禁补代码、改状态或填写根待办。

### M7-01A：建立 timeline 包骨架

单一结果：创建可构建且无业务实现的 `@voxweaver/timeline`。

- 前置：M7-G00 closed。
- 读取：根 workspace 配置和现有最小包模板。
- 允许写入：`packages/timeline/package.json`、`packages/timeline/tsconfig.json`、`packages/timeline/src/index.ts`、`packages/timeline/test/package.test.mjs`。
- 验证：`pnpm --filter @voxweaver/timeline test`；`pnpm --filter @voxweaver/timeline build`。
- 停止条件：模板要求外部依赖或修改根脚本/lint 全局策略时停止。

### M7-01B：声明 M7 workspace 依赖

单一结果：只接通 timeline、application 和 app-core 的 workspace 依赖。

- 前置：M7-01A。
- 读取：目标 package manifests 和 lockfile importer。
- 允许写入：`packages/timeline/package.json`、`packages/application/package.json`、`services/app-core/package.json`、`pnpm-lock.yaml`。
- 实现边：timeline → contracts；application → timeline；app-core → timeline；保留全部既有依赖。
- 验证：`pnpm install --lockfile-only --offline`；`pnpm run build`。
- 停止条件：需要 registry 依赖、反向依赖或无关 lockfile 差异时停止。

### M7-02：时间轴契约与 JSON Schema

单一结果：固定 ChapterTimeline、TimelineEntry 和派生导出 manifest 契约。

- 前置：M7-01B、M7-D01 closed、M7-D02 closed。
- 读取：阶段 10 第 4～7 节、M6 ChapterAssembly、ScriptUnit 和共享文本引用契约。
- 允许写入：`packages/contracts/src/timeline.ts`、`packages/contracts/src/index.ts`、`packages/contracts/test/timeline.test.mjs`、`docs/schemas/chapter-timeline.schema.json`。
- 契约：timeline/entry ID、assembly revision、ScriptUnit/speaker/display text reference、ClipRef/audio revision、整数范围、duration、schema/serializer/policy version、导出 profile 的 `subtitle_text_layer`、review/validity、导出 hash。
- 验证：最小合法对象、重复 ID/order、负数/零长度/越界/倒序/重叠策略、缺映射、未知版本/枚举、schema 与 TypeScript 一致；`pnpm --filter @voxweaver/contracts test`。
- 停止条件：需要复制 ScriptUnit/TextRange 同名类型或修改 M6 冻结契约时停止。

### M7-03：冻结时间轴与字幕夹具

单一结果：建立后续任务只读的 M7 输入、预期字节和 hash 基线。

- 前置：M7-02。
- 读取：M6 可再分发夹具和 D01 关闭记录。
- 允许写入：`packages/timeline/test/fixtures/m7-timeline/README.md`、`packages/timeline/test/fixtures/m7-timeline/manifest.json`、`packages/timeline/test/fixtures/m7-timeline/input/**`、`packages/timeline/test/fixtures/m7-timeline/expected/**`、`packages/timeline/test/m7FixtureIntegrity.test.mjs`。
- 夹具：多角色、中文标点/换行、同毫秒边界、小时边界、角色名特殊字符、无 speaker、单条/多条、章节切换、assembly 更新和 stale 变体。
- 验证：许可、文件/hash、期望 JSON/VTT/SRT 字节、范围和仅声明字段变化；`pnpm --filter @voxweaver/timeline test`。
- 停止条件：D01 profile 不完整、夹具依赖不可再分发文本或后续实现需要改 expected 时停止。

### M7-04：ChapterTimeline 构建器

单一结果：纯函数从 assembly ClipRef 与 ScriptUnit 显示投影生成 TimelineEntry 列表。

- 前置：M7-03、M7-D02 closed。
- 读取：M7 契约、M6 manifest 和只读夹具。
- 允许写入：`packages/timeline/src/chapterTimelineBuilder.ts`、`packages/timeline/src/index.ts`、`packages/timeline/test/chapterTimelineBuilder.test.mjs`。
- 验证：单/多条、speaker、显示文本映射、缺 ScriptUnit、重复 ClipRef、输入乱序、稳定 ID、相同输入确定性；`pnpm --filter @voxweaver/timeline test`。
- 停止条件：assembly 无法一一反查 ScriptUnit，或 D02 要求未规划的词级输入时停止。

### M7-05：时间轴校验与双向定位

单一结果：纯逻辑校验范围，并实现时间→条目与 ScriptUnit→时间的确定性查询。

- 前置：M7-04。
- 读取：M7 契约和只读夹具。
- 允许写入：`packages/timeline/src/timelineValidator.ts`、`packages/timeline/src/timelineLookup.ts`、`packages/timeline/src/index.ts`、`packages/timeline/test/timelineValidator.test.mjs`、`packages/timeline/test/timelineLookup.test.mjs`。
- 验证：开始/结束边界、间隙、末尾、越界、重叠策略、重复 ScriptUnit、空时间轴、二分查找与线性基准一致；`pnpm --filter @voxweaver/timeline test`。
- 停止条件：边界命中策略未在契约中定义时停止，不得让 UI 自行选择。

### M7-06：项目内 JSON 序列化器

单一结果：把完整 ChapterTimeline 确定性序列化为项目 JSON artifact。

- 前置：M7-02、M7-D01 closed。
- 读取：M7 schema 和只读 expected JSON。
- 允许写入：`packages/timeline/src/jsonTimelineSerializer.ts`、`packages/timeline/src/index.ts`、`packages/timeline/test/jsonTimelineSerializer.test.mjs`。
- 验证：稳定字段/数组顺序、UTF-8、未知字段策略、schema 校验、相同输入字节一致、hash 一致；`pnpm --filter @voxweaver/timeline test`。
- 停止条件：序列化需要丢失稳定 ID、revision 或来源字段时停止。

### M7-07：WebVTT 序列化器

单一结果：按 W3C WebVTT 语法和项目 D01 profile 输出确定性 `.vtt`。

- 前置：M7-05、M7-D01 closed。
- 读取：W3C WebVTT 规范、D01 关闭记录和只读 expected VTT。
- 允许写入：`packages/timeline/src/webVttSerializer.ts`、`packages/timeline/src/index.ts`、`packages/timeline/test/webVttSerializer.test.mjs`。
- 验证：`WEBVTT` 头、UTF-8、时间戳、单调 cue、小时边界、文本/角色转义、换行、空输入、相同输入字节一致；`pnpm --filter @voxweaver/timeline test`。
- 停止条件：D01 未定义 `subtitle_text_layer`、所选 revision 缺失行为、重叠、角色前缀或文本换行行为时停止。

### M7-08：SRT 兼容序列化器

单一结果：按 D01 固定的项目 SRT profile 输出确定性 `.srt`。

- 前置：M7-07。
- 读取：D01 关闭记录和只读 expected SRT；WebVTT 仅作时间数据输入，不作转换源。
- 允许写入：`packages/timeline/src/srtSerializer.ts`、`packages/timeline/src/index.ts`、`packages/timeline/test/srtSerializer.test.mjs`。
- 验证：cue 编号、逗号毫秒、小时边界、换行/空行、角色前缀、特殊文本、空输入、确定字节；`pnpm --filter @voxweaver/timeline test`。
- 停止条件：SRT profile 仍依赖“播放器一般支持”的模糊描述时停止。

### M7-09：时间轴导出清单

单一结果：为同一 timeline revision 生成 JSON/VTT/SRT 派生物清单和 hash 关系。

- 前置：M7-06、M7-07、M7-08。
- 读取：M7 契约和序列化器输出。
- 允许写入：`packages/timeline/src/timelineExportManifest.ts`、`packages/timeline/src/index.ts`、`packages/timeline/test/timelineExportManifest.test.mjs`。
- 验证：三个格式同源、版本/hash、缺失派生物、重复格式、显示策略变化、时间不变；`pnpm --filter @voxweaver/timeline test`。
- 停止条件：清单需要绝对路径或把 VTT/SRT 标为真值时停止。

### M7-10：时间轴 Application workflow

单一结果：Application 用 fake workflow 生成并原子提交 timeline 与三个派生导出 revision。

- 前置：M7-04、M7-05、M7-06、M7-07、M7-08、M7-09。
- 读取：M0 workflow 提交端口、M6 assembly 查询和 timeline package。
- 允许写入：`packages/application/src/timelineApplicationService.ts`、`packages/application/src/index.ts`、`packages/application/test/timelineApplicationService.test.mjs`。
- 流程：校验 session/assembly current → 创建 Task/指纹 → 构建/校验 → 写 tmp → 序列化 → commit artifacts/dependencies → 激活 timeline。
- 验证：成功、幂等、取消、序列化失败、只读、旧 session、assembly 变更、重复任务、提交前中断、旧 revision 保留；`pnpm --filter @voxweaver/application test`。
- 停止条件：必须覆盖 M6 输出或直接写 exports 正式快照时停止。

### M7-11：时间轴 App Core 集成

单一结果：真实项目目录和状态库验证 M7-10 的提交、重开与恢复。

- 前置：M7-10。
- 读取：App Core 和 project-workspace 当前实现。
- 允许写入：`services/app-core/src/appCoreService.ts`、`services/app-core/test/timeline.integration.test.mjs`。
- 验证：提交/重开、running 恢复、孤立 tmp、缺文件、只读、旧 session、历史 JSON/VTT/SRT 不覆盖；`pnpm --filter @voxweaver/app-core test`。
- 停止条件：需要第二写入口或放宽项目根路径检查时停止。

### M7-12：时间轴 stale 影响选择器

单一结果：纯函数把 assembly、显示名、章节顺序和样式变化映射为精确 timeline/export selector。

- 前置：M7-02、M7-05。
- 读取：M0 selector、M6 影响 selector 和 D01 策略。
- 允许写入：`packages/workflow-core/src/timelineImpact.ts`、`packages/workflow-core/src/index.ts`、`packages/workflow-core/test/timelineImpact.test.mjs`。
- 验证：单章 assembly、speaker 显示名、章节顺序、仅样式、两个 stale cause、内容恢复、其他章保持 current；`pnpm --filter @voxweaver/workflow-core test`。
- 停止条件：现有 selector 不能表达 chapter/timeline 交集时停止，不扩大为全书 stale。

### M7-13：时间轴局部过期集成

单一结果：Application/Core 证明单章 assembly 更新只过期对应 timeline 和字幕。

- 前置：M7-11、M7-12。
- 读取：M7-10 服务和 M7-03 变体夹具。
- 允许写入：`packages/application/test/timelineImpactApplicationService.test.mjs`、`services/app-core/test/timelineImpact.integration.test.mjs`。
- 验证：单章更新、其他章 current、显示名只重导出显示数据、样式不传播、两个 cause、历史复用；`pnpm --filter @voxweaver/application test`；`pnpm --filter @voxweaver/app-core test`。
- 停止条件：发现实现缺陷时退回 M7-10/M7-11，不得在本卡修改实现。

### M7-14：同步阅读查询与命令 DTO

单一结果：固定章节列表、timeline、播放状态、导出、跳转和返回 M6 调整所需桌面 schema；不定义 timeline mutation。

- 前置：M7-02、M7-D02 closed。
- 读取：桌面消息信封、阶段 10 阅读器要求和 playback URL 契约。
- 允许写入：`packages/contracts/src/synchronizedReading.ts`、`packages/contracts/src/index.ts`、`packages/contracts/test/synchronizedReading.test.mjs`、`docs/schemas/synchronized-reading.schema.json`。
- 验证：合法/非法 payload、项目 session、revision baseline、时间边界、导出格式枚举、只读方法、返回 M6 只包含 chapter/assembly/ScriptUnit 逻辑 ID、无 timeline mutation、播放 URL 不含绝对路径；`pnpm --filter @voxweaver/contracts test`。
- 停止条件：DTO 需要媒体字节、绝对路径、HTMLAudioElement 或任意 IPC channel 时停止。

### M7-15：同步阅读 Application 查询与命令

单一结果：实现 M7-14 的 UI 无关章节/timeline 查询、导出请求和影响预览。

- 前置：M7-11、M7-13、M7-14。
- 读取：M7 workflow、Artifact playback URL 查询和 timeline lookup。
- 允许写入：`packages/application/src/synchronizedReadingApplicationService.ts`、`packages/application/src/index.ts`、`packages/application/test/synchronizedReadingApplicationService.test.mjs`。
- 验证：章节/timeline 查询、受控 URL、JSON/VTT/SRT 导出、stale/missing、只读、baseline 冲突、旧 session、项目切换；`pnpm --filter @voxweaver/application test`。
- 停止条件：查询必须解析绝对路径或保存 Renderer 播放状态到正式领域数据时停止。

### M7-15B：同步阅读 App Core 装配

单一结果：App Core 装配 M7-14 DTO 与 M7-15 Application 服务，并用真实 project session 完成集成验证。

- 前置：M7-15。
- 允许写入：`services/app-core/src/appCoreService.ts`、`services/app-core/test/synchronizedReading.integration.test.mjs`。
- 验证：覆盖读写/只读、项目切换、旧 session、项目重开、受控播放 URL、导出请求和 revision/stale 查询一致；`pnpm --filter @voxweaver/app-core test`。
- 停止条件：需要修改 DTO/Application 实现、加入 Electron/Renderer 依赖、绕过 project session/受控 artifact URL 或写入白名单外文件时停止并退回对应任务。

### M7-16：Electron Main 同步阅读路由

单一结果：Main 校验并转发 M7 查询和导出方法。

- 前置：M7-15B；M7-G00 关闭证据中的桌面 transport/playback protocol 已验证。
- 读取：M7 DTO、Main handler 和 artifact protocol 实现。
- 允许写入：`apps/desktop/main/src/synchronizedReadingHandlers.ts`、`apps/desktop/main/src/index.ts`、`apps/desktop/main/test/synchronizedReadingHandlers.test.mjs`。
- 验证：sender/session、合法/非法消息、旧 URL、范围请求委托、Core 错误、未知方法；`pnpm --dir apps/desktop/main test`。
- 停止条件：Main 必须读取 timeline 领域文件、拼字幕或绕过 Core ID 解析时停止。

### M7-17：Preload 同步阅读桥接

单一结果：contextBridge 只暴露 M7 白名单查询、导出和事件。

- 前置：M7-16。
- 读取：M7 DTO 和 Preload 现有命名空间。
- 允许写入：`apps/desktop/preload/src/synchronizedReadingBridge.ts`、`apps/desktop/preload/src/index.ts`、`apps/desktop/preload/test/synchronizedReadingBridge.test.mjs`。
- 验证：方法白名单、非法时间/格式、事件过滤、取消订阅、旧 session、无通用 IPC/文件 API；`pnpm --dir apps/desktop/preload test`。
- 停止条件：Preload 基线不存在或要求暴露 Electron event/绝对路径时停止。

### M7-18：同步播放状态 composable

单一结果：实现与 DOM 解耦的播放时间、当前条目和双向跳转状态机。

- 前置：M7-05、M7-17。
- 读取：M7 lookup、DTO 和 Renderer 现有 Composition API 约定。
- 允许写入：`apps/desktop/renderer/src/features/reading/useSynchronizedPlayback.ts`、`apps/desktop/renderer/src/features/reading/synchronizedPlaybackState.ts`、`apps/desktop/renderer/test/synchronizedPlayback.test.mjs`。
- 验证：play/pause/seek/rate、时间→高亮、文本→seek、间隙/末尾、章节切换、项目切换清理、旧事件、stale/missing；`pnpm --dir apps/desktop/renderer test`。
- 停止条件：状态机需访问 Node/Electron、跨项目全局单例或自行加载绝对路径时停止。

### M7-19：同步阅读 Renderer 页面

单一结果：Renderer 完成最小章节播放器、文本高亮、双向跳转和字幕导出界面。

- 前置：M7-18、M7-D01 closed、M7-D02 closed。
- 读取：阶段 10 第 6 节、M7 DTO 和受控 URL 用法。
- 允许写入：`apps/desktop/renderer/src/features/reading/SynchronizedReadingView.vue`、`apps/desktop/renderer/src/features/reading/TimelineTextList.vue`、`apps/desktop/renderer/src/features/reading/ReadingTransport.vue`、`apps/desktop/renderer/src/router/index.ts`、`apps/desktop/renderer/test/synchronizedReadingView.test.mjs`。
- 验证：加载/空/错误、播放/暂停/倍速/拖动、corrected 主文本/spoken 查看切换、当前文本、点击跳转、返回 M6 调整、无 timeline 编辑入口、章节切换、stale/missing、导出、键盘可达、只读和项目切换；`pnpm --dir apps/desktop/renderer test`；`pnpm run check`。
- 停止条件：需要直接文件/IPC/Provider 访问，或 UI 试图修改时间轴而 D02 为只读时停止。

### M7-20：M7 里程碑验收

单一结果：形成 M7 的可复现证据包，不新增功能。

- 前置：M7-01A、M7-01B、M7-02、M7-03、M7-04、M7-05、M7-06、M7-07、M7-08、M7-09、M7-10、M7-11、M7-12、M7-13、M7-14、M7-15、M7-15B、M7-16、M7-17、M7-18、M7-19 complete；M7-G00、M7-D01、M7-D02 closed。
- 读取：冻结夹具、全部交接和上位验收项。
- 允许写入：仅可新建 `docs/validation/m7-acceptance.md`。
- 禁止写入：源码、schema、测试、夹具、expected、上位计划和状态。
- 验证：`pnpm run check`；`git diff --check`；真实音频桌面交互单独记录视口、步骤和项目切换。
- 必证场景：完整章节自动高亮、corrected 主显示与 spoken 查看切换、双向跳转、返回 M6 且不创建 timeline override、JSON/VTT/SRT 按批准 profile 确定导出、全链追溯、旧 revision 保留、单章 stale、跨项目隔离。
- 停止条件：缺实际播放、格式校验、项目切换或门禁证据时判定未完成并退回责任卡，不得改验收夹具。

## 8. 验收标准

- 完整章节播放时按 ScriptUnit 自动高亮；
- 阅读器默认显示 corrected，可查看 spoken，两者均保留来源 revision；
- 点击文本和拖动音频都能稳定定位另一侧；
- JSON、WebVTT、SRT 从同一 timeline revision 确定性导出；
- 任一条目可追溯到 assembly、Clip、AudioClip revision 和 ScriptUnit；
- 时间范围合法且不越过章节时长；
- assembly 更新只使对应章节 timeline/字幕过期；
- 历史 timeline 和导出不覆盖；
- 项目切换后旧播放、旧事件和旧 URL 不进入新项目；
- 任何时间调整返回 M6 重新装配，不创建 timeline override；
- MVP 不依赖字/词级对齐或可编辑时间轴。

## 9. 对下游的稳定交付

M7 向 M8 交付：

- immutable `ChapterTimeline` revision 和完整 JSON artifact；
- TimelineEntry 的稳定 ID、ScriptUnit/speaker/display text、Clip/audio 来源和整数毫秒范围；
- WebVTT/SRT 派生 artifact、serializer/profile version 和 hash；
- timeline → assembly → clip → ScriptUnit 的完整依赖链；
- 章节 duration、stale/missing 状态和精确 chapter selector；
- 受控播放 URL 能力，不含绝对路径。

不得向下游暴露播放器局部状态、DOM、绝对路径或把 VTT/SRT 当作项目真值。

## 10. 本轮核验的外部依据

- W3C WebVTT 最新发布规范：<https://www.w3.org/TR/webvtt1/>
- Electron 自定义协议与流式音视频资源：<https://www.electronjs.org/docs/latest/api/protocol>
- JSON Schema Draft 2020-12 Core：<https://json-schema.org/draft/2020-12/json-schema-core>

SRT 在本规格中采用项目自有、由 M7-D01 冻结并由夹具验证的兼容 profile；未将社区惯例表述为正式标准。
