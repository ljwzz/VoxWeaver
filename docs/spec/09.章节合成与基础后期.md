# 阶段 09：章节合成与基础后期

状态：`planned`
实现审计：`unverified`
前置阶段：[08 音频 QA](./08.音频QA.md)

## 1. 目标

把审核通过的 AudioClip 按剧本顺序装配为章节音频，保留 Clip 时间线，并执行 MVP 所需的停顿、淡化和基础响度处理。

## 2. 范围

- AudioClip 引用和顺序；
- Clip 间距、停顿和必要的短淡化；
- 基础响度检查与统一；
- 章节 WAV 或确定的中间格式；
- 片段到章节时间轴；
- 章节装配 revision、输入清单和输出哈希；
- 单章重建和片段替换。

## 3. 非目标

MVP 不包含：

- 自动 BGM、SFX 和环境音混音；
- 复杂 EQ、Compressor、De-esser、Noise Gate 等母带流程；
- 完全自动的场景音效决策；
- 全书最终容器和发布元数据，属于阶段 11。

前三项未纳入当前正式范围，只有逐项批准并先更新主计划后才可另行规划。全书最终容器和发布元数据仍属于当前 MVP 的阶段 11，只是不在阶段 09 实现。

## 4. Timeline 与 Clip

```ts
interface AudioClipRef {
  clipRefId: string;
  scriptUnitId: string;
  audioArtifactId: string;
  order: number;
  startMs: number;
  endMs: number;
  gainDb?: number;
  fadeInMs?: number;
  fadeOutMs?: number;
  pauseAfterMs?: number;
}

interface ChapterAssembly {
  assemblyId: string;
  chapterId: string;
  clipRefs: AudioClipRef[];
  processingPolicyVersion: string;
  outputArtifactId: string;
}
```

公共时间和范围字段固定使用整数毫秒半开区间 `[startMs, endMs)`。处理器内部可以使用更精细时间基准，但必须按 M6-D02 的版本化换算与舍入规则转换为公共整数毫秒字段，不得暴露混合浮点单位。

## 5. 装配流程

```text
选择当前 approved AudioClip
→ 校验 ScriptUnit 顺序和完整性
→ 应用停顿和 Clip 级设置
→ 基础响度/峰值处理
→ 章节拼接
→ 生成章节音频、Clip 时间轴和清单
→ 验证可播放、时长和反向定位
```

FFmpeg 提供 concat filter/demuxer 等拼接方式，可作为适配器候选；具体命令和是否重编码由音频格式 ADR 决定：

https://www.ffmpeg.org/faq.html

## 6. 页面和审核

- 章节波形和 Clip 时间线；
- 试听单 Clip、相邻过渡和完整章节；
- 调整停顿、增益和短淡化；
- 替换某一 ScriptUnit 的活动 AudioClip；
- 显示缺失、拒绝、过期和未审核片段；
- 只重建当前章节；
- 比较历史 assembly revision。

## 7. 版本和过期影响

- ScriptUnit 顺序、活动 AudioClip、停顿或处理策略变化使对应章节 assembly 过期；
- 单一片段变化不使其他章节过期；
- 章节标题或全书顺序变化不使章节音频过期，除非章节音频包含朗读标题；
- assembly 过期使依赖它的时间轴和发布导出过期；
- 旧章节音频和清单保留；
- 仅页面显示设置变化不传播到音频。

## 8. 早期实现迁移

检查旧实现是否：

- 通过文件名排序拼接；
- 直接引用可变音频路径；
- 混入未审核或过期片段；
- 无 Clip 到 ScriptUnit 的反向定位；
- 修改一句就重建全书；
- 后期参数无版本；
- 章节输出覆盖旧文件。

可复用的 FFmpeg/音频处理代码应包装为版本化处理器；缺少输入清单的旧章节音频作为历史外部资产保留。

## 9. 测试要求

- ScriptUnit 顺序与 Clip 顺序一致；
- 未批准、缺失和 stale 片段阻断当前正式装配；
- 停顿、淡化、增益和时长计算；
- 输出可播放、无损坏且时间轴范围有效；
- 从任意时间点可反查 Clip 和 ScriptUnit；
- 替换一个片段只重建一个章节；
- 中断不会提交半成品章节。

## 10. 验收标准

- 可以播放完整章节；
- 对白顺序正确，角色音频引用正确；
- 基础音量和过渡通过样章人工审核；
- 任一章节可独立重建；
- 章节音频可反查全部输入片段；
- 旧版本不被覆盖；
- MVP 不依赖复杂 BGM/SFX 或母带系统。

## 11. 已确认策略与执行输入

项目负责人于 2026-08-08 冻结下列策略。处理器、共享音频规格和 profile 输入未齐时相关 Gate 保持 `open`。

| Gate | 状态 | 已选策略 | 仍缺输入或禁止项 |
|---|---|---|---|
| M6-D01 | `open` | 随应用分发受控本地音频处理器；是否使用 FFmpeg 及许可/升级由 ADR 固定 | ARCH-D01/D02 平台与打包前置；accepted 处理器 ADR、来源/版本/许可/hash、授权夹具、真实 smoke 命令、判定、环境、清理和负责人 |
| M6-D02 | `open` | 与 M4 共用一次冻结的 WAV/PCM mono AudioOutputSpec | codec/container、sample rate、bit depth、channels、时间换算与校验夹具；不得保留 Provider native 作为正式中间格式 |
| M6-D03 | `open` | 响度/峰值复用 M5 的版本化 profile；停顿优先级为人工 assembly override > ScriptUnit 显式值 > 类型策略默认 | profile 数值/版本/测量实现、淡化和冲突规则、样章基准证据 |
| M6-D04 | `closed` | MVP 单轨顺序装配，只保留版本化 `AudioProcessor` port | 不实现多轨时间线或插件 SDK |

任务级阻塞、目标平台条件和关闭证据见 [M6 章节合成执行规格](../milestones/M6.章节合成执行规格.md)。
