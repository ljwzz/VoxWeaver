{
  "name": "@voxweaver/application",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist ../../node_modules/.cache/voxweaver/application.tsbuildinfo",
    "test": "tsc -p tsconfig.json && node --test test/*.test.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "typecheck:source": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@voxweaver/contracts": "workspace:*",
    "@voxweaver/novel-domain": "workspace:*",
    "@voxweaver/novel-import": "workspace:*",
    "@voxweaver/text-pipeline": "workspace:*",
    "@voxweaver/workflow-core": "workspace:*"
  }
}
