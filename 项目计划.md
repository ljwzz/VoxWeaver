# AI 有声小说制作系统项目计划

## 1. 项目目标

开发一套通用的 AI 有声小说制作系统，将小说文本自动转换为可发布、可编辑、可复用的多角色有声小说工程。

系统重点解决以下问题：

* 支持多种小说文件格式导入。
* 自动识别章节并进行文本切分。
* 自动修复错别字、标点和明显文本异常。
* 正确处理中文分词、多音字、人物称谓等问题。
* 自动识别旁白、角色对白及对白顺序。
* 自动提取角色并维护角色身份、阶段、状态。
* 主角及重要角色保持整部作品音色一致。
* 普通配角在同一场景内具备足够的声音区分度。
* 支持角色音色克隆、音色配置和必要的音色微调。
* 支持情绪、年龄、身体状态等对语音表现的影响。
* 自动生成语音并进行 ASR 回转校验。
* 检查漏字、错字、多音字、断句错误等语音质量问题。
* 支持音频剪辑、响度处理、背景音乐、环境音等后期制作。
* 输出带时间轴、文本同步阅读能力的有声内容。
* 支持工程文件导入导出，方便跨设备、跨团队继续制作。
* 导出工程时不包含受版权保护的小说原文及最终生成音频，仅保存角色、配置、处理规则和模型相关结果。

---

# 2. 项目范围

系统整体划分为以下核心模块：

1. 文件预处理
2. 文本校对
3. 小说剧本化
4. 角色分析
5. 角色声音管理
6. TTS 音频生成
7. 音频质量检测
8. 音频后期制作
9. 时间轴与同步阅读
10. 工程管理
11. 导出与发布

整体流程：

```text
小说文件
    ↓
格式解析
    ↓
章节识别
    ↓
文本清洗
    ↓
文本校对
    ↓
剧本生成
    ↓
角色识别
    ↓
角色状态分析
    ↓
声音分配
    ↓
TTS 生成
    ↓
ASR 校验
    ↓
问题片段重新生成
    ↓
音频后期
    ↓
时间轴生成
    ↓
有声阅读文件 / 发布文件
```

---

# 3. 第一阶段：文件预处理

## 3.1 输入格式

第一版优先支持：

* TXT
* EPUB
* Markdown

后续扩展：

* DOCX
* PDF
* HTML
* MOBI / AZW
* 压缩包批量导入

## 3.2 文本标准化

统一处理：

* UTF-8 编码
* BOM
* CRLF / LF
* 全角半角
* 连续空格
* 连续换行
* 特殊控制字符
* HTML 标签
* EPUB 样式内容
* 页眉页脚
* 广告文本
* 网站水印
* 重复章节标题

## 3.3 章节识别

支持规则识别：

```text
第一章
第1章
第一回
Chapter 1
卷一
第一卷 第一章
```

同时支持 AI 辅助判断无法通过规则识别的章节。

章节最终保存为结构化数据，而不是单纯拆 TXT 文件。

例如：

```json
{
  "chapterId": "chapter_0001",
  "volume": 1,
  "chapter": 1,
  "title": "第一章",
  "rawText": "...",
  "normalizedText": "..."
}
```

## 3.4 长文本切片

章节仍可能超过模型上下文，因此进一步切分为：

```text
Chapter
 ├─ Scene
 │   ├─ Segment
 │   ├─ Segment
 │   └─ Segment
 └─ Scene
```

建议三级结构：

```text
Chapter → Scene → Segment
```

而不是直接：

```text
Chapter → Text Chunk
```

Scene 将成为后续：

* 角色识别
* 配角声音分配
* 情绪识别
* BGM
* 环境音
* 音频后期

的基础处理单位。

---

# 4. 第二阶段：文本校对

原则：

> AI 不直接覆盖原始文本。

保存：

```text
rawText
normalizedText
correctedText
```

同时记录修改记录。

例如：

```json
{
  "type": "typo",
  "original": "己经",
  "replacement": "已经",
  "confidence": 0.98
}
```

## 4.1 校对内容

包括：

* 错别字
* OCR 错误
* 标点
* 重复文字
* 缺失文字推测
* 人名一致性
* 地名一致性
* 称谓一致性
* 明显语病

禁止 AI 主动进行文学改写。

## 4.2 修改等级

建议区分：

```text
SAFE
REVIEW
DANGEROUS
```

### SAFE

确定性较高：

```text
己经 → 已经
```

可自动应用。

### REVIEW

存在语境判断：

```text
他 / 她
人名同音字
```

进入人工确认。

### DANGEROUS

可能改变原文含义：

```text
补全文本
重新组织句子
```

默认禁止自动修改。

---

# 5. 第三阶段：小说剧本化

目标不是改变小说，而是将小说转换为适合 TTS 的结构。

例如原文：

```text
韩立摇了摇头，道：“不用了。”

“为什么？”厉飞雨问道。

韩立沉默片刻，道：“有些事情，你不知道更好。”
```

转换：

```json
[
  {
    "type": "narration",
    "speaker": "narrator",
    "text": "韩立摇了摇头。"
  },
  {
    "type": "dialogue",
    "speaker": "han_li",
    "text": "不用了。"
  },
  {
    "type": "dialogue",
    "speaker": "li_feiyu",
    "text": "为什么？"
  },
  {
    "type": "narration",
    "speaker": "narrator",
    "text": "韩立沉默片刻。"
  },
  {
    "type": "dialogue",
    "speaker": "han_li",
    "text": "有些事情，你不知道更好。"
  }
]
```

## 5.1 Script Unit

定义统一最小剧本单元：

```json
{
  "id": "line_001",
  "chapterId": "chapter_001",
  "sceneId": "scene_001",

  "type": "dialogue",

  "speakerId": "character_001",

  "text": "...",

  "emotion": {},
  "physicalState": {},
  "speechStyle": {},

  "pronunciation": {},

  "audio": {}
}
```

整个系统后续围绕 Script Unit 工作。

---

# 6. 第四阶段：角色提取

建立全书级别的：

```text
Character Registry
```

即角色注册表。

不能每章独立进行人物识别，否则容易出现：

```text
韩立
韩兄
韩道友
韩前辈
```

被识别为四个人。

## 6.1 Character 数据结构

```json
{
  "id": "char_han_li",

  "name": "韩立",

  "aliases": [
    "韩兄",
    "韩道友",
    "韩前辈"
  ],

  "importance": "main",

  "gender": "male",

  "stages": []
}
```

---

# 7. 角色阶段系统

同一人物的声音不一定整个故事完全不变。

例如：

```text
少年
青年
中年
老年

健康
重伤
虚弱

正常
入魔
附身
灵魂状态
```

因此定义：

```text
Character
    ↓
Character Stage
    ↓
Voice Profile
```

例如：

```json
{
  "stageId": "han_li_young",
  "characterId": "han_li",

  "ageStage": "young",

  "physicalState": "normal",

  "voiceProfileId": "voice_han_li_01"
}
```

---

# 8. 角色状态系统

状态不能直接绑定到角色。

应该绑定到具体 Script Unit。

例如：

```json
{
  "emotion": {
    "primary": "angry",
    "intensity": 0.7
  },

  "mentalState": {
    "calm": 0.2,
    "fear": 0.1
  },

  "physicalState": {
    "injured": true,
    "weakness": 0.6
  },

  "speechStyle": {
    "speed": 0.9,
    "volume": 0.8
  }
}
```

最终：

```text
角色基础音色
+
角色阶段
+
当前精神状态
+
当前身体状态
+
当前语境
=
最终 TTS 参数
```

---

# 9. 第五阶段：声音系统

声音划分为三类。

## 9.1 核心角色

例如：

```text
主角
主要配角
长期反派
长期出现角色
```

使用固定：

```text
Voice Profile
```

支持：

* 音色克隆
* 固定 Seed
* 固定 Speaker Embedding
* 固定 Voice ID
* 模型版本锁定

确保跨章节声音一致。

---

# 10. 普通配角声音池

大量配角不适合全部做音色克隆。

建立：

```text
Voice Pool
```

例如：

```text
male_young
male_middle
male_old

female_young
female_middle
female_old

child
monster
special
```

每个声音包含 metadata：

```json
{
  "voiceId": "voice_023",

  "gender": "male",

  "age": 35,

  "pitch": 0.4,

  "speed": 0.5,

  "roughness": 0.7,

  "brightness": 0.3
}
```

---

# 11. Scene Voice Assignment

同一 Scene 中出现多个角色时：

```text
计算声音之间的相似度
```

避免：

```text
角色 A ≈ 角色 B
```

导致听众无法分辨。

声音分配需要同时满足：

```text
角色特征匹配
+
当前 Scene 声音差异
+
历史声音使用情况
```

该模块可以称为：

```text
Voice Casting Engine
```

---

# 12. 第六阶段：中文发音处理

建立独立：

```text
Pronunciation Layer
```

不能完全依赖 TTS 模型自行判断。

主要处理：

* 多音字
* 人名
* 地名
* 门派
* 法术
* 法宝
* 生僻字
* 数字
* 英文
* 单位
* 特殊符号

例如：

```json
{
  "original": "重阳",
  "phoneme": "...",
  "source": "ai_context",
  "confidence": 0.95
}
```

---

# 13. 发音词典

建立项目级 Pronunciation Dictionary。

例如：

```json
{
  "entries": {
    "角色名A": "...",
    "地名B": "...",
    "术语C": "..."
  }
}
```

优先级：

```text
人工指定
>
项目词典
>
角色/世界观词典
>
AI 上下文判断
>
TTS 默认
```

一旦人工确认：

```text
全书复用
```

---

# 14. 第七阶段：TTS 生成

不建议整章直接生成。

建议按照：

```text
Script Unit
```

生成独立音频。

例如：

```text
line_0001.wav
line_0002.wav
line_0003.wav
```

最终再进行拼接。

好处：

* 修改一句无需重新生成整章
* 可以单独重新生成错误片段
* 可以调整一句话情绪
* 可进行缓存
* 支持分布式生成
* 支持失败重试

---

# 15. 音频生成缓存

建立 Hash：

```text
hash(
    text
    + voice
    + emotion
    + pronunciation
    + model
    + parameters
)
```

相同输入直接使用缓存音频。

避免重复调用模型。

---

# 16. 第八阶段：音频 QA

生成：

```text
Text
 ↓
TTS
 ↓
Audio
 ↓
ASR
 ↓
Transcript
 ↓
Text Compare
```

检测：

* 漏字
* 多字
* 错字
* 重复
* 错误断句
* 数字错误
* 人名错误
* 多音字错误
* 长时间静音
* 爆音
* 削波
* 音量异常

---

# 17. QA 分级

例如：

```text
PASS
WARNING
FAIL
```

失败时：

```text
自动重新生成
```

如果连续多次失败：

```text
进入人工审核队列
```

---

# 18. 第九阶段：音频后期制作

建立 Audio Timeline。

例如：

```text
00:00 Narrator
00:07 Character A
00:12 Character B
00:16 Narrator
```

每个 Clip：

```json
{
  "clipId": "clip_001",

  "scriptUnitId": "line_001",

  "audio": "line_001.wav",

  "start": 10.2,

  "end": 14.8
}
```

---

# 19. 后期处理能力

包括：

* 静音裁剪
* 淡入淡出
* Clip 间距调整
* 音量统一
* Loudness Normalization
* EQ
* Compressor
* De-esser
* Limiter
* Noise Gate

---

# 20. BGM / SFX

独立轨道：

```text
Narration
Dialogue
BGM
SFX
Ambient
```

例如：

```text
暴雨
雷声
脚步
森林
城市
战斗
酒楼
山洞
```

Scene 可以自动生成：

```text
Audio Scene Metadata
```

例如：

```json
{
  "environment": "forest",
  "weather": "rain",
  "mood": "tense"
}
```

用于推荐：

```text
BGM
Ambient
SFX
```

建议第一阶段只实现自动推荐，不直接完全自动混音。

---

# 21. 第十阶段：时间轴与同步阅读

最终输出包含：

```text
Audio Timeline
+
Text Timeline
```

每条 Script Unit 具有：

```text
startTime
endTime
```

例如：

```json
{
  "text": "不用了。",
  "speaker": "韩立",

  "start": 12.35,
  "end": 13.92
}
```

播放器即可实现类似：

```text
播客逐字稿
歌词
听书同步阅读
```

的体验。

---

# 22. 同步阅读格式

第一阶段优先支持：

```text
JSON
WebVTT
SRT
```

例如：

```text
WEBVTT

00:00:12.350 --> 00:00:13.920
韩立：不用了。
```

后续可以自定义：

```text
Audiobook Project Format
```

---

# 23. 阅读器能力

最终阅读播放器建议支持：

* 播放 / 暂停
* 倍速
* 跳转
* 章节切换
* 文本同步高亮
* 当前角色显示
* 角色头像
* 当前 Scene
* 书签
* 笔记
* 字体大小
* 夜间模式
* 点击文字跳转音频
* 点击时间轴跳转文本

---

# 24. 第十一阶段：工程系统

项目不应仅保存最终音频。

应该保存完整制作工程。

例如：

```text
Novel Project
```

结构：

```text
project/
├── project.json
│
├── chapters/
│
├── scripts/
│
├── characters/
│
├── voices/
│
├── pronunciation/
│
├── timelines/
│
├── metadata/
│
└── cache/
```

---

# 25. Project Metadata

```json
{
  "projectVersion": "1.0",

  "title": "...",

  "language": "zh-CN",

  "chapters": [],

  "characters": [],

  "voices": [],

  "pronunciationDictionary": {}
}
```

---

# 26. 工程导出

定义：

```text
.audiobook-project
```

本质可以采用：

```text
ZIP + JSON
```

导出包含：

* 项目配置
* 角色列表
* 角色别名
* 角色阶段
* 角色状态规则
* Voice Profile
* Speaker Embedding
* 音色微调结果
* 发音词典
* Prompt
* 模型配置
* Scene Metadata
* Script Metadata
* Timeline Metadata

默认不包含：

* 小说原文
* 最终生成音频
* BGM
* SFX
* 缓存音频

---

# 27. 工程导入

导入工程后：

```text
重新导入小说
```

系统根据：

```text
章节
文本 Hash
角色
Script Metadata
```

进行匹配。

然后恢复：

* 角色配置
* 音色
* 发音词典
* 场景配置
* 情绪参数
* 模型设置

最终重新生成音频。

---

# 28. 系统核心数据模型

建议核心实体：

```text
Project

Book
Volume
Chapter
Scene
ScriptUnit

Character
CharacterAlias
CharacterStage

VoiceProfile
VoicePool

PronunciationEntry

AudioClip
AudioTimeline

QAResult
```

关系：

```text
Project
 └ Book
    └ Chapter
       └ Scene
          └ ScriptUnit
             ├ Character
             ├ CharacterStage
             ├ VoiceProfile
             ├ Pronunciation
             ├ AudioClip
             └ QAResult
```

---

# 29. 系统架构

建议采用：

```text
Desktop / Web UI
        │
        ▼
Application API
        │
        ▼
Workflow Engine
        │
        ├── Document Service
        ├── NLP Service
        ├── LLM Service
        ├── Character Service
        ├── Voice Service
        ├── TTS Service
        ├── ASR Service
        ├── Audio Service
        └── Export Service
```

底层模型全部通过 Adapter 抽象：

```text
LLMProvider
TTSProvider
ASRProvider
EmbeddingProvider
```

从而允许：

```text
本地模型
云 API
不同厂商
```

自由切换。

---

# 30. Workflow Engine

该项目本质是一个长流水线任务系统，因此必须支持：

```text
任务状态
失败恢复
断点续传
并发控制
缓存
重试
依赖关系
任务日志
```

例如：

```text
Chapter Pipeline

Parse
 ↓
Correct
 ↓
Script
 ↓
Character Analyze
 ↓
Pronunciation
 ↓
TTS
 ↓
ASR QA
 ↓
Audio Process
 ↓
Timeline
```

任何一个节点都应该可以单独重新执行。

---

# 31. 人工审核机制

不应该追求：

```text
100% AI 自动完成
```

而应该设计：

```text
AI 自动处理
+
低置信度人工审核
```

主要审核队列：

```text
文本修改
角色识别
角色归属
多音字
发音
角色阶段
角色状态
音色分配
ASR 错误
```

例如：

```text
Confidence >= 0.95
    自动通过

0.7 - 0.95
    建议审核

< 0.7
    强制审核
```

---

# 32. MVP 范围

第一版不要直接实现完整生产系统。

建议 MVP 目标：

```text
TXT / EPUB
↓
章节识别
↓
文本校对
↓
剧本化
↓
角色提取
↓
角色音色配置
↓
TTS
↓
ASR QA
↓
章节音频
↓
字幕 / 时间轴
```

暂时不做：

* 自动 BGM
* 自动 SFX
* 自动复杂混音
* 大规模 Voice Fine-tune 管理
* 移动端
* 在线协作
* 云端大规模任务调度

---

# 33. MVP 里程碑

## M0：项目基础设施

完成：

* Project 数据结构
* Chapter 数据结构
* ScriptUnit 数据结构
* Character 数据结构
* VoiceProfile 数据结构
* 本地项目目录
* 数据库
* Job 系统

---

## M1：小说导入

完成：

```text
TXT / EPUB
↓
章节识别
↓
文本标准化
```

验收标准：

* 小说可以完整导入。
* 章节顺序正确。
* 原始文本不会丢失。
* 可以查看每个章节。

---

## M2：文本 AI 处理

完成：

```text
文本校对
剧本生成
角色识别
```

验收标准：

* 能区分旁白和对白。
* 能识别大部分角色。
* 能识别角色别名。
* 所有 AI 修改可以追溯。

---

## M3：角色系统

完成：

* Character Registry
* Alias
* Character Stage
* Voice Profile
* Voice Pool

验收标准：

* 主角跨章节保持统一角色 ID。
* 主角声音跨章节保持一致。
* Scene 中普通配角声音能够明显区分。

---

## M4：语音生成

完成：

```text
ScriptUnit
↓
Pronunciation
↓
TTS
↓
AudioClip
```

验收标准：

* 每句话独立生成。
* 可单独重新生成。
* 支持缓存。
* 支持不同 Voice Profile。

---

## M5：音频 QA

完成：

```text
Audio
↓
ASR
↓
文本比对
```

验收标准：

能发现：

* 漏字
* 错字
* 重复
* 明显发音错误

失败片段可以重新进入 TTS。

---

## M6：章节合成

完成：

```text
AudioClip
↓
Timeline
↓
Chapter Audio
```

验收标准：

* 对白顺序正确。
* 角色声音一致。
* 音量基本统一。
* 可以播放完整章节。

---

## M7：同步阅读

完成：

```text
Audio Timeline
+
Text Timeline
```

支持：

```text
JSON
WebVTT
SRT
```

验收标准：

播放器中：

```text
音频播放
↓
对应文本自动高亮
```

---

## M8：工程导入导出

完成：

```text
.audiobook-project
```

支持：

* 导出工程配置。
* 导入工程。
* 重新绑定小说。
* 恢复角色。
* 恢复音色。
* 恢复发音词典。
* 重新生成音频。

---

# 34. 第二阶段功能

MVP 稳定后增加：

## 音频后期

* Compressor
* EQ
* Limiter
* Loudness
* Noise Control

## Scene Audio

* 环境识别
* Ambient 推荐
* SFX 推荐
* BGM 推荐

## Voice Casting

根据：

```text
年龄
性别
身份
性格
当前 Scene
```

自动分配声音。

## 角色关系图谱

自动生成：

```text
Character Graph
```

例如：

```text
师徒
朋友
敌人
家族
门派
阵营
```

用于帮助 AI 理解后续章节。

---

# 35. 第三阶段功能

成熟版本进一步增加：

* 分布式 TTS
* GPU Worker
* 模型管理
* 多用户
* 在线项目
* 多人协作
* 云端渲染
* 模型市场
* Voice Library
* BGM Library
* SFX Library
* 插件系统
* API
* CLI
* SDK

---

# 36. 项目界面建议

整体建议采用专业音频制作软件的布局。

```text
┌─────────────────────────────────────────┐
│                 Toolbar                 │
├──────────┬────────────────────┬─────────┤
│          │                    │         │
│ Chapter  │      Script        │ Detail  │
│          │                    │         │
│ Scene    │ Dialogue           │Character│
│          │ Narration          │Emotion  │
│          │                    │Voice    │
├──────────┴────────────────────┴─────────┤
│               Timeline                  │
├─────────────────────────────────────────┤
│ Player                                  │
└─────────────────────────────────────────┘
```

主要页面：

```text
Project

Script Editor

Character Manager

Voice Manager

Pronunciation Dictionary

Audio Timeline

QA Review

Export
```

---

# 37. 关键技术原则

整个系统建议遵循以下原则。

### 原文不可破坏

所有处理均建立在：

```text
Raw
↓
Normalized
↓
Corrected
↓
Script
```

多层数据之上。

任何 AI 处理必须可以回退。

### AI 输出结构化

尽量避免 AI 返回自由文本。

统一使用：

```text
JSON Schema
```

### 模型可替换

LLM、TTS、ASR 不与业务逻辑强绑定。

### 最小单位可重新生成

音频最小生成单位为 Script Unit，而不是章节。

### 所有 AI 结果带置信度

低置信度结果进入审核流程。

### 保证可复现性

保存：

```text
Model
Model Version
Prompt Version
Seed
Voice Profile
Parameters
```

### Pipeline 可断点恢复

任何阶段失败都不应导致章节从头重新处理。

---

# 38. 最核心的四个子系统

如果从工程实现优先级考虑，整个项目最关键的不是 TTS 本身，而是以下四个系统。

## Character Registry

解决：

```text
这个人到底是谁？
```

包括：

* Character
* Alias
* Stage
* Relationship

## Script Engine

解决：

```text
这句话是谁说的？
```

## Voice Casting Engine

解决：

```text
这个角色应该使用什么声音？
```

## Pronunciation Engine

解决：

```text
这句话应该怎么念？
```

TTS 只是上述结构最终的执行器。

---

# 39. 推荐开发优先级

建议实际开发顺序：

```text
Project Model
        ↓
Document Parser
        ↓
Chapter / Scene
        ↓
Script Engine
        ↓
Character Registry
        ↓
Pronunciation Engine
        ↓
Voice Profile
        ↓
TTS Adapter
        ↓
ASR QA
        ↓
Timeline
        ↓
Audio Post Processing
        ↓
Project Export
```

不要从：

```text
TTS
```

开始开发。

否则很快会遇到：

```text
谁在说话？
这个角色是不是之前出现过？
为什么声音变了？
这个多音字怎么读？
为什么重新生成整章？
怎么只修改一句？
```

这些问题。

---

# 40. 项目最终形态

项目最终不应只是：

> 小说转语音工具

而应该定位为：

> AI Audiobook Production Studio

即：

```text
小说解析
+
AI 剧本系统
+
角色管理
+
AI 配音
+
音色管理
+
发音管理
+
质量检测
+
音频制作
+
同步阅读
+
工程管理
```

形成完整的 AI 有声内容生产工作站。
