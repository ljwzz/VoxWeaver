# VoxWeaver 项目计划

状态：产品主计划
版本：1.1
更新日期：2026-08-09

## 1. 文档定位

本文件是 VoxWeaver 产品目标、范围、MVP 和阶段顺序的唯一主文档。详细实现要求由 [docs/README.md](./docs/README.md) 路由到对应阶段和里程碑；技术选型由 ADR 记录；未确认想法保存在 `docs/ideas/`。

## 2. 项目目标

开发一套通用的 Electron 桌面端 AI 有声小说生产工程，将合法取得的小说文本转换为可编辑、可审核、可复现、可局部重处理的多角色有声内容。

系统重点解决：

- 多种小说格式导入、章节识别和结构化切分；
- 原文保护、文本清洗、校对和修改审计；
- 旁白、对白、内心独白和说话人解析；
- 全项目角色、别名、阶段和状态管理；
- 核心角色音色一致性及普通角色区分度；
- 中文专名、多音字、数字和单位的发音控制；
- ScriptUnit 级音频生成、缓存、重试和替换；
- ASR 回听、音频检测和人工复核；
- 章节装配、音文时间轴和同步阅读；
- 工程配置导入导出和可追溯发布快照；
- 前序修改后的最小范围过期传播，旧产物不强制删除。

## 3. 非目标

- 不承诺全自动、零人工审核地完成整部长篇作品；
- 不内置或分发未经授权的小说、真人声音、模型权重、BGM 或 SFX；
- 不把某个 LLM、TTS、ASR、数据库或前端框架固化为唯一实现；
- 不内置、下载、加载或托管 LLM、TTS、ASR、VAD 或说话人分析模型，所有模型能力通过外部 Provider API 接入（见 [ADR 0001](./docs/adr/0001-model-capabilities-via-provider-apis.md)）；
- 不开发、部署或维护独立网页端；Vue Renderer 只作为 Electron 桌面应用界面；
- 不在 MVP 实现移动端、在线多人协作和云端大规模调度；
- 不把自动说话人标签直接视为真实角色；
- 不把 `docs/ideas/` 内容自动升级为正式范围。

## 4. 产品流程

```text
项目创建与素材登记
→ 小说格式解析
→ canonical / normalized
→ 章节、Scene、ProcessingSegment
→ corrected 文本
→ ScriptUnit 和说话人候选
→ Character Registry 和角色阶段
→ VoiceProfile / VoicePool / 发音词典
→ ScriptUnit 级 TTS
→ ASR 与音频 QA
→ 章节合成与基础后期
→ 时间轴与同步阅读
→ 工程包和发布快照
```

任何阶段都必须保留输入版本和来源。用户返回前序阶段时创建新 revision，并只将实际依赖旧输入的后续产物标记为 `stale`。

## 5. 阶段计划

| 阶段 | 目标 | 正式文件 |
|---|---|---|
| 00 | 项目、目录、状态、依赖、任务和迁移基线 | [docs/spec/00](./docs/spec/00.项目基线与工程工作区.md) |
| 01 | 小说导入、标准化、章节、Scene 和技术分块 | [docs/spec/01](./docs/spec/01.小说导入与文件预处理.md) |
| 02 | 可审计文本校对 | [docs/spec/02](./docs/spec/02.文本校对.md) |
| 03 | 结构化剧本和说话人候选 | [docs/spec/03](./docs/spec/03.小说剧本化.md) |
| 04 | 角色、别名和长期阶段 | [docs/spec/04](./docs/spec/04.角色注册表与状态.md) |
| 05 | 音色档案、声音池和 Scene 分配 | [docs/spec/05](./docs/spec/05.声音系统与音色分配.md) |
| 06 | spoken 文本和中文发音 | [docs/spec/06](./docs/spec/06.中文发音处理.md) |
| 07 | 多后端 TTS 和片段任务 | [docs/spec/07](./docs/spec/07.TTS生成.md) |
| 08 | ASR、音频 QA 和人工审核 | [docs/spec/08](./docs/spec/08.音频QA.md) |
| 09 | 章节合成和 MVP 基础后期 | [docs/spec/09](./docs/spec/09.章节合成与基础后期.md) |
| 10 | 音文时间轴和同步阅读 | [docs/spec/10](./docs/spec/10.时间轴与同步阅读.md) |
| 11 | 工程导入导出和发布 | [docs/spec/11](./docs/spec/11.工程导入导出与发布.md) |

MVP 纵向联调和量化验收见 [docs/spec/90.MVP联调与验收.md](./docs/spec/90.MVP联调与验收.md)。

## 6. 核心数据模型

```text
Project
├── SourceAsset
├── Book
│   └── Volume
│       └── Chapter
│           └── Scene
│               ├── ProcessingSegment
│               └── ScriptUnit
├── Character
│   ├── CharacterAlias
│   └── CharacterStage
├── VoiceProfile
├── VoicePool
├── PronunciationEntry
├── AudioClip
├── QAResult
├── ChapterAssembly
├── ChapterTimeline
├── Artifact / ArtifactRevision / ArtifactDependency
└── ExportSnapshot
```

`ProcessingSegment` 服务模型上下文和任务调度；`ScriptUnit` 服务剧本、角色、发音和音频生成。角色长期阶段与 ScriptUnit 瞬时情绪、身体状态必须分离。

## 7. MVP

MVP 目标：完成一个合法授权、约 30 分钟、角色不超过 5 个的样章闭环。

### M0 项目基础设施

项目目录、核心实体、状态库、Task/StageRun、产物版本和依赖机制可运行。

### M1 小说导入

TXT 完整导入，章节顺序、原文定位和覆盖率正确。EPUB 与 Markdown 仍属于阶段 01 的后续能力，但不纳入 MVP M1 验收。

### M2 文本 AI 处理

Scene/ProcessingSegment 桥接以及校对、剧本、角色和别名结果可审核、可追溯。

### M3 角色系统

核心角色跨章节保持稳定 ID 和音色，Scene 内普通角色可以区分。

### M4 语音生成

每个已审核 ScriptUnit 基于其 spoken revision 与已分配 VoiceProfile 独立生成 AudioClip，并支持缓存、取消和有限重试。

### M5 音频 QA

问题片段可检测、人工复核并返回正确前序阶段。

### M6 章节合成

审核通过片段可以生成完整章节音频并反查 ScriptUnit。

### M7 同步阅读

音频播放时文本同步，支持 JSON、WebVTT 和 SRT。

### M8 工程导入导出

工程配置可以导出、重新导入、绑定合法小说并恢复生成配置。

## 8. MVP 暂不实施

- 自动 BGM、SFX、环境音和复杂混音；
- 大规模音色微调管理；
- 完整角色关系图谱和人物时间线；
- DOCX、PDF/OCR、MOBI/AZW 和压缩包批量导入；
- 独立网页端、移动端、在线项目和多人协作；
- 云端渲染、模型市场、插件市场和 SDK。

## 9. MVP 后续阶段

MVP 数据证明可行后，只从 `docs/ideas/` 中逐项评审候选能力。未经用户明确批准的具体想法不构成项目计划或实施顺序。

## 10. 关键原则

### 原文不可破坏

```text
source_asset
→ extracted/raw
→ canonical
→ normalized
→ corrected
→ script
→ spoken
```

任何 AI 或人工处理都不得覆盖前一层正式 revision。

### 适配器隔离外部系统

小说格式、外部 LLM/TTS/ASR 等 Provider API、音频处理和存储通过稳定端口接入；领域模型不包含具体厂商私有字段。

### 结构化 AI 输出

AI 返回候选、边界、分类、证据和置信度，并通过 schema 校验；程序负责切分、应用 diff 和持久化。

### 最小单元重处理

文本按稳定块处理，音频按 ScriptUnit 生成，章节按 Clip 装配。局部变化不得强制重建整书。

### 产物可追溯

每个产物保存输入、模型/处理器版本、Prompt、参数、Seed 策略、音色、词典、哈希、操作者和审核。

### 增量过期而非级联删除

前序新版本只使实际依赖它的产物过期；旧产物保留用于比较、复用和审计。

### 人工审核是正式流程

低置信度、未知说话人、危险文本修改、音色授权和 QA 异常必须有明确人工入口。

## 11. 当前状态

当前仓库已完成并验证 M0 项目基础设施：项目目录、读写/只读会话、单写锁与失效锁恢复、SQLite 状态库、核心记录、Task/StageRun、Artifact revision、依赖图、增量过期、迁移、备份和恢复端口均可运行。阶段 00 桌面纵向闭环也已验证：Electron 项目页、最近项目入口以及 Renderer → Preload → Main → MessagePort → Core 跨进程链路已在未签名 macOS arm64 开发 `.app` 中通过实际 E2E；该证据不包含 maker、签名、公证、发布、更新或回退验收。M1-G00 已关闭，但 M1～M8 和阶段 01～11 仍未验证。所有实现结论必须基于代码、测试和运行证据，不得根据文档内容推断功能完成。
