# VoxWeaver 实施计划

## 目标

先完成一个约 30 分钟、角色不超过 5 个的合法授权样章，验证从文本导入到 M4B 导出的完整闭环；根据实测人工修改率、重生成率、专有名词错误率和处理成本决定是否扩展到整卷或整书。

本计划只定义交付物、依赖关系和验收条件。具体框架、模型和阈值通过 ADR 与基准测试确定。

## 基线原则

- 原始文本和原始音频只读，衍生物使用内容哈希关联。
- 所有自动修改、模型调用、人工审核和状态转换可审计。
- 所有模型后端通过稳定适配器接入，不让业务状态依赖单一模型。
- 逐片段生成、检查、重试和替换，不整章一次生成。
- 先验证授权与可再分发范围，再导入任何真实素材。
- 先完成纵向样章，再扩充角色数、模型数和部署规模。

## 阶段总览

| 阶段 | 交付目标 | 前置条件 | 完成标志 |
|---|---|---|---|
| P0 | 项目基线与授权治理 | 无 | ADR、数据规则、验收指标获确认 |
| P1 | 可运行的最小纵向切片 | P0 | 文本可导入并生成单角色章节音频 |
| P2 | 文本校正与知识库 | P1 | 原文、校正文、朗读文本可追踪 |
| P3 | 多角色脚本与审核 | P2 | 旁白/台词/角色归属可编辑和复核 |
| P4 | 发音与文本前端 | P2 | 术语、数字、多音字可锁定并回归 |
| P5 | 多后端 TTS 与角色音色 | P3、P4 | 可按角色生成且可复现 |
| P6 | 自动 QA 与人工复核 | P5 | 问题片段可定位、退回和重生成 |
| P7 | 章节合成与 M4B/MP3 导出 | P6 | 样章成品与元数据通过验收 |
| P8 | 扩展与生产化 | P7 | 一卷级压力测试达到预算目标 |

## P0：项目基线与授权治理

### 任务

- 初始化版本库、许可证、贡献指南和安全说明。
- 创建 ADR，确定：
  - Python 和 Node.js 的版本策略；
  - Web/API/worker 的边界；
  - SQLite 起步还是直接使用 PostgreSQL；
  - 本地队列还是 Redis 类队列；
  - 对象存储接口与本地文件布局；
  - 首批 LLM、TTS、ASR 和文本前端候选。
- 定义素材登记表：来源、权利人、许可范围、到期时间、是否允许训练、是否允许克隆、是否允许发布。
- 选定合法授权样章、旁白音色和不超过 4 个角色音色。
- 定义样章基准和人工审核表。

### 验收

- 未登记授权状态的素材不能进入处理队列。
- 原始素材目录默认被 `.gitignore` 排除。
- 每个候选模型记录版本、许可证、模型卡和运行环境。
- 样章验收指标、统计方法和失败处置规则书面确认。

## P1：最小纵向切片

### 任务

- 实现 TXT/Markdown 导入和章节切分。
- 建立项目、章节、片段、角色、任务、资产的最小数据模型。
- 实现一个 TTS 适配器和一个本地串行 worker。
- 生成单旁白片段并合成为章节 WAV。
- 提供最小 API 或 CLI：导入、生成、查询状态、重试、导出。
- 保存文本哈希、模型版本、参数、随机种子和输出哈希。

### 验收

- 同一输入与固定配置可追踪到唯一的生成记录。
- 单个失败片段可以独立重试，不重新生成整章。
- 中断后能从未完成片段继续。
- 原始输入、生成片段和章节输出之间可双向定位。

## P2：文本校正与知识库

### 任务

- 实现 `original`、`corrected`、`spoken` 三层文本。
- 实现人物、别名、实体、禁改词和发音词典的导入与版本化。
- 实现分块校正，并携带必要的前后文。
- 校正结果输出原文、修正文、理由、置信度和定位。
- 高置信度修改可批量应用；其他修改进入复核队列。
- 为重复字、漏字、标点和术语误改建立回归夹具。

### 验收

- 任何校正都可撤销，且不会覆盖原始文本。
- 相同术语在全书范围使用统一实体标识。
- 未达到自动应用阈值的修改不会进入 `corrected`。
- 测试夹具能检测专有名词被通用纠错器误改的情况。

## P3：多角色脚本与审核

### 任务

- 定义旁白、对话、内心独白、群体发言和未知说话人类型。
- 第一遍抽取说话人、情绪、动作和上下文。
- 第二遍校验角色在场、连续轮次、称谓、时间线和首次出场。
- 实现低置信度规则和人工审核队列。
- Web 端支持逐句修改角色、文本、情绪、语速、音量和停顿。
- 保存模型建议与人工最终决定，供后续评估。

### 验收

- 所有台词都有明确角色、旁白或 `unknown` 标记。
- `unknown` 和低置信度片段不能直接进入批量 TTS。
- 修改角色归属后只使受影响片段失效。
- 能统计角色识别准确率、人工修改率和主要错误类型。

## P4：发音与文本前端

### 任务

- 接入文本归一化接口，处理数字、日期、百分数、单位和缩写。
- 接入 G2P/多音字候选实现。
- 实现项目词典覆盖、上下文读音和人工锁定机制。
- 为每次处理记录词典版本和规则版本。
- 建立专有名词、常见多音字、儿化、变调和数字读法测试集。

### 验收

- 同一个锁定术语在全部章节使用相同读音。
- 原文、朗读文本和音素/拼音表示可互相定位。
- 词典变更只使实际受影响的片段重新排队。
- 样章首次出现的所有专有名词完成人工确认。

候选中文文本前端的官方文档：

https://paddlespeech.readthedocs.io/en/latest/tts/zh_text_frontend.html

## P5：多后端 TTS 与角色音色

### 任务

- 定义统一 TTS 请求、响应、错误和能力描述。
- 首批适配一个零样本/声音设计后端和一个可微调后端。
- 实现角色音色档案、参考音频版本和情绪参考选择。
- 实现短语义片段切分、并发限制、超时和可控重试。
- 对后端版本升级执行固定样本 A/B 测试。
- 对角色参考音频执行来源登记和质量分级。

### 验收

- 更换 TTS 后端不改变结构化脚本格式。
- 每段音频可追踪到模型、参考音频、参数、种子和文本版本。
- 后端升级不会覆盖旧音频或旧审核结论。
- 参考音频未通过授权和质量检查时不能被任务引用。

首批候选后端的官方项目：

https://github.com/QwenLM/Qwen3-TTS

https://github.com/RVC-Boss/GPT-SoVITS

## P6：自动 QA 与人工复核

### 任务

- 接入 ASR 回听并计算文本差异。
- 对人名、术语、数字和单位设置加权错误。
- 检测削波、静音、截断、异常响度、异常时长和重复音频。
- 可选接入说话人嵌入相似度。
- 定义自动通过、人工复核、自动重试和永久失败策略。
- 审核端支持试听、波形定位、文本对照、重生成和批量通过。

### 验收

- 每个片段有机器检查结果和最终审核结论。
- 自动重试存在次数上限，且保留每次失败原因。
- 人工可以只重生成一个片段或某角色的选定片段。
- 基于样章数据校准阈值，并记录误报率和漏报率。

候选 ASR/说话人工具的官方项目：

https://github.com/modelscope/FunASR

https://github.com/pyannote/pyannote-audio

## P7：章节合成与导出

### 任务

- 根据标点、段落、场景和角色切换插入可配置停顿。
- 对片段执行必要的淡化与响度统一。
- 生成章节 WAV，并保留片段时间轴。
- 导出 M4B/MP3，写入标题、作者、封面和章节标记。
- 对导出物检查章节位置、总时长、可播放性和元数据。
- 生成项目清单：文本版本、词典版本、模型版本、审核时间和文件哈希。

### 验收

- 任意章节可独立重新合成和替换。
- 导出前不得包含未通过审核的片段。
- 导出文件可在至少两个独立播放器中读取章节信息。
- 从成品时间点可以反查结构化脚本片段。

FFmpeg 官方拼接说明：

https://www.ffmpeg.org/faq.html

## P8：扩展与生产化

### 任务

- 从样章扩展到一卷，验证数千片段的调度和恢复。
- 引入并行 worker、资源池、优先级和幂等任务。
- 将数据库和对象存储替换为可扩展实现，但保持接口不变。
- 增加备份、迁移、磁盘配额、指标、日志和告警。
- 建立角色音色漂移、模型升级和词典变更的回归集。
- 统计每万字耗时、失败率、人工分钟数、GPU/CPU 成本和存储增长。

### 验收

- worker 重启、任务重复投递和网络中断不会产生错误状态或覆盖已批准资产。
- 能按项目、章节、角色、后端和错误类型观察处理状态。
- 一卷级实测成本与人工投入在 P0 确定的预算内。
- 扩展前的样章结果可继续复现。

## 样章验收指标

P0 必须确定具体目标值；以下只定义指标，不预设未经实测的阈值：

- 文本自动修改接受率与人工撤销率；
- 说话人识别准确率、低置信度覆盖率和人工修改率；
- 专有名词、多音字、数字和单位的错误率；
- ASR 字符错误率及其对真实听感问题的召回率；
- 每片段平均生成次数和永久失败率；
- 角色音色一致性人工评分；
- 每千字人工审核分钟数；
- 每千字 CPU/GPU 时间、峰值显存和存储占用；
- 章节导出成功率与播放器兼容性。

## 首个里程碑：30 分钟样章

1. 确认文本、音色和发布范围的授权。
2. 选择人物不超过 5 个、包含旁白与不同情绪的章节。
3. 建立旁白、主要角色和通用角色音色。
4. 完成知识库、保守校正和结构化脚本。
5. 完成数字、多音字和专有名词人工锁定。
6. 逐片段生成并运行自动 QA。
7. 在审核端完成试听、修正和必要的重生成。
8. 输出章节 WAV 和带章节信息的 M4B。
9. 统计全部验收指标和主要失败类型。
10. 形成继续、调整或停止扩展的决策记录。

## 待决策事项

- Web 技术栈与组件库；
- API 框架和数据库；
- 本地任务队列与分布式队列的切换边界；
- 首批 LLM、TTS、ASR、文本前端及固定版本；
- 音频中间格式、采样率和响度目标；
- 说话人识别与角色身份映射的人工流程；
- 自动校正、自动通过和自动重试阈值；
- 模型权重、声音资产和项目数据的加密与访问控制；
- 本地 Apple Silicon 与远程 CUDA 节点的任务分工；
- 项目开源许可证和可公开测试素材的许可。

